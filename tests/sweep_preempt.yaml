# 4 items, all sharing one server config — exercises server reuse.
sweep_id: preempt-test
model: fake-model
port: 18000

server_warmup_timeout_s: 30
bench_timeout_s: 60
max_attempts_per_item: 3

defaults:
  server:
    warmup_s: 0.5

items:
  - id: item1
    server: {}
    bench:
      script: fake
      duration_s: 1.5

  - id: item2
    server: {}
    bench:
      script: fake
      duration_s: 1.5

  - id: item3
    server: {}
    bench:
      script: fake
      duration_s: 1.5

  - id: item4
    server: {}
    bench:
      script: fake
      duration_s: 1.5
